define(["backbone", "lodash"], function(Backbone, _) {
	//教师有多个班级
	//一个班级中有多个学生
	//一场考试有多个科目
});

/*
复杂的view需要复杂的model的结构方式
var model = new Backbone.Model();
model.set({contacts: new ContactsModel(), users: new UsersModel()});
var view = new GridView({model: model})
*/

